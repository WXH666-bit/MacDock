<Window x:Class="MacDock.UI.Views.MenuBarWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:MacDock.UI.Converters"
        WindowStyle="None"
        Background="Transparent"
        AllowsTransparency="False"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Topmost="True"
        ShowActivated="False">
    <Window.Resources>
        <conv:IconStateToGeometryConverter x:Key="IconToGeometry"/>
        <BooleanToVisibilityConverter x:Key="BoolToVisibility"/>

        <!-- 托盘图标项模板：可见区与溢出区共用，点击转发由 code-behind 处理 -->
        <DataTemplate x:Key="TrayItemTemplate">
            <Border Margin="5,0"
                    Width="24" Height="24"
                    Background="Transparent"
                    CornerRadius="5"
                    Cursor="Hand"
                    ToolTip="{Binding Tooltip}"
                    MouseLeftButtonUp="OnTrayMouseLeft"
                    MouseRightButtonUp="OnTrayMouseRight">
                <Image Source="{Binding Icon}" Width="16" Height="16"/>
            </Border>
        </DataTemplate>
    </Window.Resources>
    <!--
        顶部菜单栏：主屏通栏，贴顶 y=0，宽度=主屏全宽。
        AllowsTransparency=False 是为了让 DWM/Accent 亚克力生效（分层窗口不支持）。
        尺寸与位置由 code-behind 用物理像素通过 SetWindowPos 落位，规避 DPI 取整露缝。
    -->
    <WindowChrome.WindowChrome>
        <WindowChrome GlassFrameThickness="-1"
                      CaptionHeight="0"
                      ResizeBorderThickness="0"
                      UseAeroCaptionButtons="False"/>
    </WindowChrome.WindowChrome>

    <Grid x:Name="RootGrid">
        <!-- 亚克力之上叠一层极淡渐变提亮，与 Dock 玻璃条同一套观感 -->
        <Border x:Name="GlassLayer">
            <Border.Background>
                <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
                    <GradientStop Color="#14FFFFFF" Offset="0"/>
                    <GradientStop Color="#08FFFFFF" Offset="1"/>
                </LinearGradientBrush>
            </Border.Background>
        </Border>

        <!-- 下边缘一根发丝分割线 -->
        <Border Height="1" VerticalAlignment="Bottom"
                Background="{DynamicResource MenuBarHairlineBrush}"/>

        <Grid Margin="8,0,8,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- 左：Logo + 前台应用名 -->
            <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                <Border x:Name="LogoButton"
                        Width="22" Height="22"
                        Background="Transparent"
                        CornerRadius="5"
                        Cursor="Hand"
                        ToolTip="关于本机"
                        MouseLeftButtonUp="OnLogoClick">
                    <Image Width="16" Height="16"
                           Source="{StaticResource MacDockLogo}"
                           Stretch="Uniform"
                           SnapsToDevicePixels="True"
                           RenderOptions.BitmapScalingMode="HighQuality"/>
                </Border>

                <Border x:Name="LaunchpadButton"
                        Width="24" Height="22" Margin="5,0,0,0"
                        Background="Transparent" CornerRadius="5"
                        Cursor="Hand" ToolTip="启动台"
                        MouseLeftButtonUp="OnLaunchpadClick">
                    <UniformGrid Width="13" Height="13" Columns="3" Rows="3">
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                        <Ellipse Width="2.5" Height="2.5" Fill="{DynamicResource MenuBarForegroundBrush}"/>
                    </UniformGrid>
                </Border>

                <TextBlock Text="{Binding ForegroundAppName}"
                           Foreground="{DynamicResource MenuBarForegroundBrush}"
                           FontSize="13"
                           FontWeight="Bold"
                           VerticalAlignment="Center"
                           Margin="8,0,0,0"
                           MaxWidth="420"
                           TextTrimming="CharacterEllipsis"/>
            </StackPanel>

            <!-- 右：托盘区（音量左侧）+ 控制中心（音量/亮度）+ 时间 -->
            <StackPanel Grid.Column="2"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        HorizontalAlignment="Right">
                <!-- 托盘区（接管任务栏托盘，TrayTakeover=false 时整块隐藏） -->
                <StackPanel x:Name="TrayRegion"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Visibility="{Binding IsTrayEnabled, Converter={StaticResource BoolToVisibility}}">
                    <!-- 可见托盘图标 -->
                    <ItemsControl ItemsSource="{Binding Visible}"
                                  ItemTemplate="{StaticResource TrayItemTemplate}"
                                  VerticalAlignment="Center"/>

                    <!-- 溢出 chevron（有隐藏图标时显示，点击弹 Popup） -->
                    <Border x:Name="OverflowChevron"
                            Visibility="{Binding HasOverflow, Converter={StaticResource BoolToVisibility}}"
                            Width="18" Height="24"
                            Background="Transparent"
                            CornerRadius="4"
                            Cursor="Hand"
                            ToolTip="隐藏的图标"
                            MouseLeftButtonUp="OnChevronClick">
                        <!-- 上折角箭头（^）：矢量 Path -->
                        <Path Width="9" Height="9"
                              Stretch="Uniform"
                              Fill="{DynamicResource MenuBarSecondaryBrush}"
                              Data="M0.5,6 L4.5,2 L8.5,6 L7.3,7.2 L4.5,4.4 L1.7,7.2 Z"/>
                    </Border>

                    <!-- 溢出图标弹层：StaysOpen=False 点外部自动关，背景深色卡片与音量浮窗一致 -->
                    <Popup x:Name="OverflowPopup"
                           StaysOpen="False"
                           AllowsTransparency="True"
                           Placement="Bottom"
                           PlacementTarget="{Binding ElementName=OverflowChevron}"
                           HorizontalOffset="-8" VerticalOffset="4">
                        <Border CornerRadius="10"
                                Background="{DynamicResource GlassSurfaceStrongBrush}"
                                BorderThickness="1"
                                BorderBrush="{DynamicResource GlassBorderBrush}"
                                Padding="8,10"
                                Margin="0,4,0,0">
                            <ItemsControl ItemsSource="{Binding Overflow}"
                                          ItemTemplate="{StaticResource TrayItemTemplate}">
                                <ItemsControl.ItemsPanel>
                                    <ItemsPanelTemplate>
                                        <WrapPanel Orientation="Horizontal" MaxWidth="240"/>
                                    </ItemsPanelTemplate>
                                </ItemsControl.ItemsPanel>
                            </ItemsControl>
                        </Border>
                    </Popup>
                </StackPanel>

                <!-- M5 控制中心：系统入口、音量、亮度与主题。 -->
                <Border x:Name="ControlCenterButton"
                        Width="28" Height="24"
                        Background="Transparent"
                        CornerRadius="5"
                        Cursor="Hand"
                        ToolTip="控制中心"
                        MouseLeftButtonUp="OnControlCenterClick">
                    <Canvas Width="16" Height="14">
                        <Path Stroke="{DynamicResource MenuBarForegroundBrush}" StrokeThickness="1.5"
                              StrokeStartLineCap="Round" StrokeEndLineCap="Round"
                              Data="M1,3.5 H15 M1,7 H15 M1,10.5 H15"/>
                        <Ellipse Width="3.5" Height="3.5" Fill="{DynamicResource MenuBarForegroundBrush}"
                                 Canvas.Left="4" Canvas.Top="1.75"/>
                        <Ellipse Width="3.5" Height="3.5" Fill="{DynamicResource MenuBarForegroundBrush}"
                                 Canvas.Left="10" Canvas.Top="5.25"/>
                        <Ellipse Width="3.5" Height="3.5" Fill="{DynamicResource MenuBarForegroundBrush}"
                                 Canvas.Left="6" Canvas.Top="8.75"/>
                    </Canvas>
                </Border>

                <!-- 亮度（不支持时整个隐藏） -->
                <Border x:Name="BrightnessIcon"
                        Visibility="{Binding IsBrightnessAvailable, Converter={StaticResource BoolToVisibility}}"
                        Width="26" Height="24"
                        Background="Transparent"
                        CornerRadius="5"
                        Cursor="Hand"
                        ToolTip="亮度"
                        MouseLeftButtonUp="OnBrightnessClick"
                        MouseWheel="OnBrightnessWheel">
                    <!-- 太阳：四芒星，代表亮度调节 -->
                    <Path Width="15" Height="15"
                          Stretch="Uniform"
                          Fill="{DynamicResource MenuBarForegroundBrush}"
                          Data="M12,4 L13.7,10.3 L20,12 L13.7,13.7 L12,20 L10.3,13.7 L4,12 L10.3,10.3 Z"/>
                </Border>

                <!-- 音量：喇叭图标 + 百分比（无音频设备时整块隐藏，与亮度图标语义一致） -->
                <Border x:Name="VolumeIcon"
                        Visibility="{Binding IsAudioAvailable, Converter={StaticResource BoolToVisibility}}"
                        Width="26" Height="24"
                        Background="Transparent"
                        CornerRadius="5"
                        Cursor="Hand"
                        ToolTip="音量"
                        MouseLeftButtonUp="OnVolumeClick"
                        MouseWheel="OnVolumeWheel">
                    <Path Width="15" Height="15"
                          Stretch="Uniform"
                          Fill="{DynamicResource MenuBarForegroundBrush}"
                          Data="{Binding VolumeIconState, Converter={StaticResource IconToGeometry}}"/>
                </Border>

                <TextBlock Text="{Binding VolumePercent}"
                           Visibility="{Binding IsAudioAvailable, Converter={StaticResource BoolToVisibility}}"
                           Foreground="{DynamicResource MenuBarSecondaryBrush}"
                           FontSize="12"
                           VerticalAlignment="Center"
                           Margin="0,0,10,0"
                           MinWidth="28"/>

                <!-- 右：时间 -->
                <TextBlock Text="{Binding ClockText}"
                           Foreground="{DynamicResource MenuBarForegroundBrush}"
                           FontSize="13"
                           VerticalAlignment="Center"/>
            </StackPanel>
        </Grid>
    </Grid>
</Window>
