using System.Windows.Controls;

namespace MacDock.UI.Controls;

/// <summary>
/// Dock 单个图标控件：显示图标、响应左键启动与右键移除。
/// </summary>
public partial class DockIconControl : UserControl
{
    public DockIconControl()
    {
        InitializeComponent();
    }
}
